/**
 * This API triggers UAC when necessary
 *
 * @param {string} cmd
 * @param {string[]} args
 * @param {import('node:child_process').ExecOptions & {timeoutMs?: number}} opts
 * @returns {Promise<{stdout: string; stderr: string;}>}
 * @throws {import('node:child_process').ExecException}
 *
 * Notes:
 * - If the UAC prompt is cancelled by the user, Start-Process returns a non-zero exit code.
 */
export function runElevated(cmd: string, args?: string[], opts?: import("node:child_process").ExecOptions & {
    timeoutMs?: number;
}): Promise<{
    stdout: string;
    stderr: string;
}>;
/**
 *
 * @param {string} srcUrl
 * @param {string} dstPath
 * @returns {Promise<void>}
 */
export function downloadToFile(srcUrl: string, dstPath: string): Promise<void>;
//# sourceMappingURL=utils.d.ts.map